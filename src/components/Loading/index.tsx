import { LoadingOutlined } from "@ant-design/icons"

const Loading: React.FC = () => {
    return (
        <div className="h-screen w-screen flex items-center justify-center w-full">
            <LoadingOutlined/>
        </div>
    )
}

export default Loading